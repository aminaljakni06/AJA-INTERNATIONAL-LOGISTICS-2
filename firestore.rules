rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Direct client access is denied. Application access goes through server
    // API routes that use the Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
